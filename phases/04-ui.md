# Phase 4 — UI (static)

**Depends on:** **0** + **1**. Browse/`getConfig` needs **3**; until then hardcode a fake path.  
**Goal:** main screen looks like the spec. Capture button does **not** run Playwright yet (or no-ops).

**Parallel:** with **2** and with **3** config/dialog. Stay in `ui/` only. Split: CSS tokens vs HTML layout vs paste-parse (copy parse rules from phase 2 or share `src/urls.js` later). Settings card markup can start here (phase **6** logic later).

Follow [00-design.md](00-design.md). Tokens in `ui/styles.css`. Logic in `ui/app.js`.

## Do

- Paste well, placeholder “Paste URLs, one per line”
- On input/paste: parse (same rules as capture) → count pill (animate) + duplicates caption
- Segmented **Images** | **Images + HTML**
- Folder row: truncated path from `getConfig` + **Browse** → `selectFolder`
- **Capture** enabled only if ≥1 URL and folder set
- Gear visible; panel can be a hidden empty card (wire in phase 6)
- Running/done markup can exist hidden (phase 5)

No React. No new packages.

## Done when

Window matches the layout sketch. Paste a list → count + dupe warning. Browse updates the path. Theme still light-only.

## Test

- [ ] Light tokens, 20px margins, 32px Capture
- [ ] Duplicate lines show “N duplicates removed”
- [ ] Capture disabled when well empty
